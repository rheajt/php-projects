var main = function() {
    var currentWord = 0;

    displayCurrentWord(currentWord);

    

};

var displayCurrentWord = function(curr) {

};

var nextWord = function(num) {
    return num + 1;
};

$(document).ready(main);